# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
*.egg

# Virtual environments
.venv/
venv/
env/
ENV/

# Test / coverage / caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
htmlcov/
.tox/

# PyCharm / IntelliJ
.idea/

# Poetry (project uses pip + pyproject; lockfile not tracked)
poetry.lock

# OS cruft
.DS_Store
Thumbs.db

# How to reach the production server: addresses, key paths, secrets. The
# secret-free deployment assets under deploy/ ARE tracked — this is only the
# operator's private address book.
SERVER.md

# run-mm-companion skill driver screenshots
_driver_shots/

# Built installers — publish these as GitHub Release assets, not in git history
# (each ~80 MB build would otherwise be stored forever). The build scripts and
# spec under installer/ stay tracked; only the generated output/ is ignored.
installer/output/
